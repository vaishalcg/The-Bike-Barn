@model IEnumerable<WebMVC.Models.Stock>

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Store_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Store_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditStoreByStoreId", new { storeId = item.Store_Id , prodId = item.Product_Id }) |
            @Html.ActionLink("Details", "Details", new { storeId = item.Store_Id, prodId = item.Product_Id }) |
            @Html.ActionLink("Delete", "Delete", new { storeId = item.Store_Id, prodId = item.Product_Id })
        </td>
    </tr>
}

</table>
